name: LINE Notify
description: LINE Notify for GitHub Actions (deprecated)
inputs:
  access_token:
    description: LINE Access Token
    required: true
  message:
    description: Message
    required: true
runs:
  using: node20
  main: index.js
branding:
  icon: message-circle
  color: green
